version: 1
frontend:
  phases:
    build:
      commands:
        - echo 'No frontend build required - static files'
  artifacts:
    baseDirectory: src/main/resources/static
    files:
      - '**/*'
